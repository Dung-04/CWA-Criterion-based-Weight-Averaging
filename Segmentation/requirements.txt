# CWA - Instance Segmentation (YOLO-seg)
# ultralytics pulls in torch, torchvision, numpy, matplotlib, PyYAML...
# The version is pinned because the pipeline relies on internal Ultralytics
# callbacks, the checkpoint format and SegmentMetrics.
ultralytics==8.3.152
pandas>=2.0.0
openpyxl>=3.1.0
PyYAML>=6.0
